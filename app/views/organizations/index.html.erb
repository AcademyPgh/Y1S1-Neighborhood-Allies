<h1>Organizations#index</h1>
<p>Listing of Every Organization</p>

<% @orgs.each do |organization| %>
<%= organization.name %> <%= link_to "Visit", organization_path(organization) %> <br />
<% end %>
